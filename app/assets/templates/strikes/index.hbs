{{#unless strikes.length}}
  <h1 class="text-center">Loading...</h1>
{{/unless}}

{{#if strikes.length}}
  <h1 class="text-center">Drone Map</h1>
  <div id="map_canvas"></div>
  <p>{{strikes.status}}</p>
  <p>{{strikes.length}}</p>
  <ul id="strikes"></ul>
{{/if}}